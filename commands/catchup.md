---
description: Token-cheap session bootstrap — what changed and where things stand
---

Get me oriented in this repo without burning context:

1. `git log --oneline -10` and `git status` — summarize, don't dump.
2. If a CI config or TODO/ROADMAP file exists, skim it via the `scout` subagent.
3. Tell me in <10 lines: current branch state, recent themes of work, anything
   broken or in-flight, and the obvious next action.

Do not read large files into context — delegate scanning to `scout`.
